/**
 * Provider credentials are server-managed. Account access and entitlements are
 * handled by AccountDialog, so the former client-side API-key gate is retired.
 */
export function PrerequisitesChecker() {
  return null
}
